language: python
python:
  - "2.7"

sudo: false

addons:
  apt:
    packages:
    - liblapack-dev
    - libblas-dev
    - gfortran

install:
  - python setup.py install

script:
  - python setup.py test
